# Data

Static or generated data files used by the app or tooling.

- **`cf_problems.json`** – Codeforces problem metadata (if used by scripts or legacy tooling; not referenced by the main app build).
